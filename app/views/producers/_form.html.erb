<%= form_with(model: producer, local: true) do |form| %>
  <% if producer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(producer.errors.count, "error") %> prohibited this producer from being saved:</h2>

      <ul>
      <% producer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :producer_id %>
    <%= form.text_field :producer_id, id: :producer_producer_id %>
  </div>

  <div class="field">
    <%= form.label :group_number %>
    <%= form.number_field :group_number, id: :producer_group_number %>
  </div>

  <div class="field">
    <%= form.label :account %>
    <%= form.text_field :account, id: :producer_account %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
