<%= form_with(model: purshase_order, local: true) do |form| %>
  <% if purshase_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purshase_order.errors.count, "error") %> prohibited this purshase_order from being saved:</h2>

      <ul>
      <% purshase_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
