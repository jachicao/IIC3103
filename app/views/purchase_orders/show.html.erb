<p id="notice"><%= notice %></p>

<p>
  <strong>Grupo Cliente:</strong>
  <%= @response['cliente'] %>
</p>

<p>
  <strong>Grupo Proveedor:</strong>
  <%= @response["cliente"] %>
</p>

<p>
  <strong>sku:</strong>
  <%= @response["sku"]%>
</p>

<p>
  <strong>Precio unitario:</strong>
  <%= @response["precioUnitario"]%>
</p>

<p>
  <strong>Cantidad:</strong>
  <%= @response["cantidad"]%>
</p>

<p>
  <strong>Estado:</strong>
  <%= @response["estado"]%>
</p>

<%= link_to 'Accept', { :controller => :purchase_orders, :action => :accept, :id => @purchase_order.po_id, :client_id => Producer.where(producer_id: @response["cliente"]).first().group_number}, {:method => :patch } %>|
<%= link_to 'Reject', { :controller => :purchase_orders, :action => :reject, :id => @purchase_order.po_id, :client_id => Producer.where(producer_id: @response["cliente"]).first().group_number}, {:method => :patch } %>|
<%= link_to 'Back', purchase_orders_path %>
